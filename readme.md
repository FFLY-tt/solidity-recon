# 🛡️ Solidity Recon: AI-Driven Smart Contract Audit Agent
> 基于 LangGraph 和 LLM 的智能合约自动化红蓝攻防审计系统

[![Python](https://img.shields.io/badge/Python-3.10%2B-blue)](https://www.python.org/)
[![LangGraph](https://img.shields.io/badge/AI-LangGraph-orange)](https://github.com/langchain-ai/langgraph)
[![Foundry](https://img.shields.io/badge/Tool-Foundry-black)](https://getfoundry.sh/)
[![Slither](https://img.shields.io/badge/Tool-Slither-green)](https://github.com/crytic/slither)

## 📖 项目简介

**Solidity Recon** 是一个自动化的智能合约安全审计代理系统。它模拟了真实的安全审计流程，结合了传统的静态分析工具、动态模糊测试工具以及大语言模型（LLM）的推理能力。

系统包含两个核心 AI Agent：
* **🔴 红队 Agent (Red Team)**: 负责“攻”。根据静态分析报告（Slither）编写针对性的 Foundry 攻击脚本，试图攻破合约。
* **🔵 蓝队 Agent (Blue Team)**: 负责“防”。分析红队的攻击日志和漏洞原理，对合约代码进行自动修复，并确保不破坏原有业务逻辑。

整个流程由 **LangGraph** 状态机调度，自动在扫描、攻击、修复和验证之间流转，直到合约通过所有安全测试。

## ✨ 核心特性

* **🕵️ 深度静态侦察**: 集成 `Slither`，自动提取漏洞位置、类型和影响范围，生成 LLM 可读的侦察报告。
* **⚔️ 动态模糊攻击**: 自动生成 Foundry (`Forge`) 测试脚本，执行 1,000 ~ 10,000 次模糊测试 (Fuzzing) 以挖掘深层逻辑漏洞。
* **🤖 智能闭环修复**: 蓝队 Agent 能够理解编译器报错和测试失败日志，进行多轮自我修正，直到代码通过编译并防御成功。
* **🐳 全沙箱环境**: 所有的编译、分析和测试任务均在 Docker 容器中运行，确保宿主机环境安全且一致。

## 🏗️ 系统工作流



```mermaid
graph TD
    Start([🚀 启动]) --> StaticScan[🔍 Slither 静态扫描]
    
    StaticScan -- 发现漏洞 --> RedTeam[🔴 红队编写攻击脚本]
    StaticScan -- 无明显漏洞 --> Fuzz1[🎲 第一轮模糊测试 (1k runs)]
    
    RedTeam --> BlueTeam[🔵 蓝队进行代码修复]
    
    Fuzz1 -- 测试通过 --> Fuzz2[🎲 第二轮模糊测试 (10k runs)]
    Fuzz1 -- 测试失败 --> BlueTeam
    
    BlueTeam --> CompileCheck{编译检查}
    CompileCheck -- 失败 --> BlueTeam
    CompileCheck -- 成功 --> StaticScan
    
    Fuzz2 -- 测试通过 --> End([✅ 审计完成 / 生成安全合约])
    Fuzz2 -- 测试失败 --> BlueTeam